// Convex Auth configuration.
//
// Full provider wiring (email / wallet login) lands in Phase 7. The empty
// providers list is valid and keeps the backend running with anonymous access
// until then. "Add Google login" / "add wallet login" become prompts, not config.
export default {
  providers: [],
};
